---
title: Release
description: How to release apps for Android and iOS
---
## Change app name
1. For Android, change the name in `android/app/src/main/AndroidManifest.xml`
   ```xml
    <manifest xmlns:android="http://schemas.android.com/apk/res/android">
        <application
            android:label="flutter_starter_kit"
            android:name="${applicationName}"
            android:icon="@mipmap/launcher_icon">
        </application>
    </manifest>
    ```
2. For iOS, change the name in `ios/Runner/Info.plist`
    ```xml
    <key>CFBundleName</key>
    <string>${applicationName}</string>
    ```

## References
1. https://docs.flutter.dev/deployment/android
2. https://docs.flutter.dev/deployment/ios
